parameters:
  - name: configuration
    displayName: Configuration
    type: string
    default: smoke-qa
    values:
      - qa
      - smoke-qa
      - smoke-ultra
      - qa-api
      - qa-dashboard
      - qa-kanban
      - qa-meeting
      - qa-report
      - qa-user
      - qa-workflow

trigger:
  branches:
    include:
      - master

schedules:
  - cron: '0 18 * * 1-5'
    displayName: 'Nightly Build at 18:00'
    always: true
    branches:
      include:
        - master

jobs:
- job: ViewpointAutomation
  timeoutInMinutes: 90
  pool: 'Build Pipelines VMSS - QAUAT'
  steps:
  - script: |
      npx cypress cache clear
    displayName: 'Cypress cache clear'

  - script: |
      npm ci
    displayName: 'npm ci'

  - script: |
      export cypress_Internal_Admin_Username=$(InternalAdminUsername)
      export cypress_Internal_Admin_Password=$(InternalAdminPassword)
      export cypress_External_Username=$(ExternalUsername)
      export HTTP_PROXY=http://10.71.1.42:3128
      npx cypress run --env configFile=${{ parameters.configuration }} --browser chrome --reporter junit
    displayName: 'Running Cypress Tests'

  - task: PublishPipelineArtifact@1
    displayName: 'Publish Cucumber Report'
    inputs:
      targetPath: 'test-results/cucumber/cucumber_report.html'
      artifact: Report
    continueOnError: true
    condition: succeededOrFailed()

  - task: PublishTestResults@2
    displayName: 'Publish Test Results'
    inputs:
      testResultsFormat: 'JUnit'
      testResultsFiles: '**/result-*.xml'
      searchFolder: '$(System.DefaultWorkingDirectory)/test-results/tests-output'
      mergeTestResults: true
      failTaskOnFailedTests: true
    condition: succeededOrFailed()
